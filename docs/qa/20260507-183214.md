# QA レポート 2026-05-07 18:32:14

## メタ情報
- ブランチ: issue-1-mvp-converter
- 対象: PR #2 再レビューコメント対応
- 実行日時: 2026-05-07 18:32:14 JST

## 対応確認
- `*_summarizedActivities.json` の root 配列構造（`[{ summarizedActivitiesExport: [...] }]`）を拾う stream path を追加
- `@streamparser/json` の selector 仕様に合わせ、`$[*]` ではなく `$.*.summarizedActivitiesExport.*` を使用
- Worker完了時はZIPを `ArrayBuffer` として返し、transfer listでMainへ渡すよう修正
- JSZip private APIの `_data.uncompressedSize` と `internalStream` 依存を撤去
- 大容量JSONの `JSON.parse` fallbackを撤去し、未対応構造はwarningにする方針へ変更
- `ACTIVITY_COLUMNS` を実データの正規化名に合わせて `avg_speed` に修正
- `recordsFromStreamValue` の到達不能コードを削除

## 実行結果
- `npm run lint`: pass
- `npm run build`: pass
- `npm audit --audit-level=moderate`: pass（0 vulnerabilities）
- `curl -I http://localhost:3000`: pass（HTTP 200）

## 合成ZIP検証
- root配列形式の `*_summarizedActivities.json` で `activities.csv` が1 row出力されることを確認
- `avgSpeed` が `avg_speed` 列に出力されることを確認
- `splits` / `uuid` が `activities.csv` に含まれないことを確認
- `startTimeGmt: 1693865432000.0` がISO文字列として出力されることを確認
