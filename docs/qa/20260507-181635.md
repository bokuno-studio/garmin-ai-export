# QA レポート 2026-05-07 18:16:35

## メタ情報
- ブランチ: issue-1-mvp-converter
- 対象: PR #2 コメント対応
- 実行日時: 2026-05-07 18:16:35 JST

## 対応確認
- JSON変換本体をWeb Workerへ移動し、UIスレッド上での大容量JSON parseを回避
- `@streamparser/json` と JSZip internal stream で既知Garmin JSON配列をストリーム処理
- `splits` / `splitSummaries` / `uuid*` / `userProfileId` / `deviceId` / `manufacturer` 等をCSV出力から除外
- 数値タイムスタンプをISO文字列へ変換
- 処理中のdrop/再実行をガードし、古い変換結果を無視
- UploadedFiles nested ZIPはアーカイブ単位で順次展開・FIT解析
- warning表示に残件数を追加
- 空CSVにデータ種別ごとのヘッダーを付与
- エラー時のStatusスピナー残留を解消

## 実行結果
- `npm run lint`: pass
- `npm run build`: pass
- `npm audit --audit-level=moderate`: pass（0 vulnerabilities）
- `curl -I http://localhost:3000`: pass（HTTP 200）

## 合成ZIP検証
- `*_summarizedActivities.json` / `*_sleepData.json` / `UDSFile_*.json` を含む合成ZIPで変換実行
- 出力:
  - `activities.csv`: 1 row
  - `sleep.csv`: 1 row
  - `daily_health.csv`: 1 row
  - `laps.csv`: 0 rows（FITなしのため空CSVヘッダーのみ）
- `activities.csv` に `splits` / `splitSummaries` / `uuid` / `deviceId` / `manufacturer` が含まれないことを確認
- `startTimeGmt` がISO文字列として出力されることを確認
